# Git Housekeeping

Run git cleanup audit with optional verbose flag.

Execute: `python scripts/git-cleanup-audit.py $ARGUMENTS`

Examples:
- `/housekeeping` - runs normally
- `/housekeeping --verbose` or `/housekeeping -v` - runs with verbose output
